---
// Text — the reading voice (body) on authored elements, heading shapes included (ADR-0023);
// the UI voices are Caption's, the loud ones Heading's.
// Same contract-map pattern as Typography: every size × element renders, so drift in
// the body bundle shows here first.
import Heading from "../components/Heading/Heading.astro";
import Prose from "../components/Prose/Prose.astro";
import Text from "../components/Text/Text.astro";
import Caption from "../components/Caption/Caption.astro";

const sizes = ["lg", "md", "sm"];
const ELEMENTS = ["p", "span", "div", "legend", "figcaption", "label", "h1", "h2", "h3", "h4", "h5", "h6"];
const VARIANTS = ["body"];
---

<section class="KitchenSink-section" id="Text">
  <Heading text="Text" element="h2" size="2" />

  <Prose variant="basic">
    <p>
      Authored single-run reading text. Text owns the body voice; Caption
      owns the UI voices (label, button); Heading owns the loud ones. The
      litmus: do you author the element? → family component. A flow you
      don't control? → Prose.
    </p>
  </Prose>

  <section class="component-map">
    <header>
      <Heading element="h3" text="runs and chaining (the run engine laws)" />
      <Prose variant="basic" size="sm">
        <p>
          One voice + inline emphasis · two voices chained on one line (a
          block host and an inline run) · voice sovereignty (an unvoiced
          span inherits, a voiced child owns its bundle).
        </p>
      </Prose>
    </header>
    <table>
      <tbody>
        <tr>
          <th scope="row">inline emphasis</th>
          <td>
            <Text size="lg"><strong>Upptäckt ett fel?</strong> Kontakta oss.</Text>
          </td>
        </tr>
        <tr>
          <th scope="row">two-voice chain</th>
          <td>
            <Text element="p" variant="body" size="lg">
              <Caption element="span" variant="label" size="lg" text="Upptäckt ett fel?" /> Kontakta oss.
            </Text>
          </td>
        </tr>
        <tr>
          <th scope="row">sovereignty</th>
          <td>
            <Text element="p" variant="body" size="md">
              An unvoiced <span>span inherits the run</span>; a voiced child
              <Caption element="span" variant="label" size="md" text=" owns its own bundle" />.
            </Text>
          </td>
        </tr>
      </tbody>
    </table>
  </section>

  {
    VARIANTS.map((variant) => (
      <section class="component-map">
        <header>
          <Heading element="h3" text={`variant: ${variant}`} />
          <Prose variant="basic" size="sm">
            <p>the body bundle — Noto Serif · p / span / div / legend / figcaption / label · size lg–sm</p>
          </Prose>
        </header>
        <table>
          <thead>
            <tr>
              <th />
              {ELEMENTS.map((el) => (
                <th scope="col">{el}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {sizes.map((size) => (
              <tr>
                <th scope="row">size {size}</th>
                {ELEMENTS.map((el) => (
                  <td>
                    <Text element={el} variant={variant} size={size} text={`${variant} ${size}`} />
                  </td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </section>
    ))
  }
</section>
