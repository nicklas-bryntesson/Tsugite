---
import Heading from "tsugite/components/Heading/Heading.astro";
---
{/* href wraps the text in a link inside the heading — the link is the run,
    so trim and size apply to it exactly as to plain text. */}
<Heading text="A heading that is also a link" href="/map" element="h2" size="2" />
