---
import Heading from "tsugite/Heading/Heading.astro";
---
{/* highlight marks the listed words (comma-separated); highlightStyle says how.
    The list of styles is open: a value is a row in the table and a rule in Heading.css. */}
<Heading
  text="A deterministic system is built from closed branches, not clever ones."
  highlight="deterministic, closed branches"
/>
<Heading
  text="A deterministic system is built from closed branches, not clever ones."
  highlight="deterministic, closed branches"
  highlightStyle="underline"
/>
