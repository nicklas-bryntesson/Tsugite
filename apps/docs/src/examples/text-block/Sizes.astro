---
import TextBlock from "tsugite/components/TextBlock/TextBlock.astro";
---
{/* preamble is the default voice — the standfirst under a heading, with its
    own leading. Three steps, md by default. */}
<TextBlock text="Size lg — a standfirst that leads the whole page." size="lg" />
<TextBlock text="Size md — the default standfirst, under a section heading." />
<TextBlock text="Size sm — a short deck above a card group." size="sm" />
